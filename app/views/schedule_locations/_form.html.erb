<%= form_for [@location, @schedule_location] do |f| %>
  <% if schedule_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule_location.errors.count, "error") %> prohibited this schedule_location from being saved:</h2>

      <ul>
      <% schedule_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :location_id, { :value => @location.id } %>

  <div class="field">
    <%= f.label :day_of_week %>
    <%= f.select :day_of_week, ScheduleLocation::DAYS_OF_WEEK %>
  </div>

  <div class="field">
    <%= f.label :open %>
    <%= f.time_select :open %>
  </div>

  <div class="field">
    <%= f.label :close %>
    <%= f.time_select :close %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
